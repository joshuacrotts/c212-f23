public class MultiplicationTable {




















//    static String multTable(int a, int b) {
//        String acc = "";
//        // Nested loops
//        // Be careful when typing or copy-pasting!
//        // Very easy to accidentally write i++ in the inner loop.
//        // Number of iterations = (a - i + 1) * (b - j + 1)
//        for (int i = 1; i <= a; i++) {
//            for (int j = 1; j <= b; j++) {
//                acc += String.format("%d*%d=%d,", i, j, i*j);
//            }
//        }
//        return acc.substring(0, acc.length() - 1);
//    }
}
