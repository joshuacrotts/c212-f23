public class TestingMethods {
    
    public static ? fiToCm(?, ?) {
        // TODO.
    }
    
    public static ? combineDigits(?, ?) {
        // TODO.
    }
    
    public static ? sumOfCubes(?, ?) {
        // TODO.
    }
}
